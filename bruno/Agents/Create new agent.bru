meta {
  name: Create new agent
  type: http
  seq: 1
}

post {
  url: {{url}}/api/agents
  body: multipartForm
  auth: none
}

body:multipart-form {
  name: testing
}

vars:post-response {
  token: res.body.token
}
